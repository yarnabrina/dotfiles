# clearing shell
alias cls="clear"

# human-friendly df
alias df="df -h"

# exit shell
alias e="exit"

# human-friendly free
alias free="free -h"

# abbreviated git
alias g="git"

# coloured grep
alias grep="grep --color=auto"

# map each line
alias map="xargs -n 1"

# directory creation
alias md="mkdir -p"

# allow sudo of aliases
alias sudo="sudo "  # space at end attempts substitution
