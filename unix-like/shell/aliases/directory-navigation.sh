# navigate to parent directory
alias ..="cd .."

# navigate to parent of parent directory
alias ...="cd ../.."

# navigate to previous directory
alias -- -="cd -"  # alias is just -, -- is used to escape
