# use pip through moodule instead of as binary
alias pip="python3 -m pip"

# abbreviation for installation
alias ppa="python3 -m pip install"

# abbreviation for uninstallation
alias ppd="python3 -m pip uninstall"
